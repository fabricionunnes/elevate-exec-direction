name: Deploy Edge Functions

on:
  push:
    branches: [main]
    paths:
      - 'supabase/functions/**'
      - '.github/workflows/deploy-functions.yml'
  workflow_dispatch:

jobs:
  # ── UNV project (czmyjgdixwhpfasfugkm) ────────────────────────────────────
  deploy-unv:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: 2.22.6

      - name: Deploy agente-unv
        continue-on-error: true
        run: supabase functions deploy agente-unv --project-ref czmyjgdixwhpfasfugkm --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy mastermind-submit
        continue-on-error: true
        run: supabase functions deploy mastermind-submit --project-ref czmyjgdixwhpfasfugkm --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

  # ── Nexus / elevate-exec-direction (xrncvhzxjmddqluxoosu) ─────────────────
  deploy-nexus:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: 2.22.6

      - name: Deploy agente-unv
        continue-on-error: true
        run: supabase functions deploy agente-unv --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy meta-ads-callback
        continue-on-error: true
        run: supabase functions deploy meta-ads-callback --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy meta-ads-sync
        continue-on-error: true
        run: supabase functions deploy meta-ads-sync --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy system-api
        run: supabase functions deploy system-api --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy financial-api
        continue-on-error: true
        run: supabase functions deploy financial-api --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy nsm-api
        continue-on-error: true
        run: supabase functions deploy nsm-api --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy database-backup
        continue-on-error: true
        run: supabase functions deploy database-backup --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy database-restore
        continue-on-error: true
        run: supabase functions deploy database-restore --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy create-job-opening
        continue-on-error: true
        run: supabase functions deploy create-job-opening --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy automation-engine
        continue-on-error: true
        run: supabase functions deploy automation-engine --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy evolution-api
        continue-on-error: true
        run: supabase functions deploy evolution-api --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt --import-map supabase/functions/deno.json
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy evolution-webhook
        continue-on-error: true
        run: supabase functions deploy evolution-webhook --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy asaas-subscription
        continue-on-error: true
        run: supabase functions deploy asaas-subscription --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy asaas-webhook
        continue-on-error: true
        run: supabase functions deploy asaas-webhook --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt --import-map supabase/functions/deno.json
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy generate-invoices
        continue-on-error: true
        run: supabase functions deploy generate-invoices --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy survey-sender
        continue-on-error: true
        run: supabase functions deploy survey-sender --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy nps-public
        continue-on-error: true
        run: supabase functions deploy nps-public --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy csat-public
        continue-on-error: true
        run: supabase functions deploy csat-public --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Deploy onboarding-ai-chat
        continue-on-error: true
        run: supabase functions deploy onboarding-ai-chat --project-ref xrncvhzxjmddqluxoosu --no-verify-jwt
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
